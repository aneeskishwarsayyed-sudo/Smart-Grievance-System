# 1. Local Database Connection
# Update 'smart_grievance' to your actual database name
spring.datasource.url=jdbc:mysql://localhost:3306/smart_grievance?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
# Enter your local MySQL password here
spring.datasource.password=K@192601a

# 2. Driver and Dialect
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# 3. Hibernate Settings
# 'update' will sync your Java code with the existing database tables
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# 4. Server Port
server.port=8081